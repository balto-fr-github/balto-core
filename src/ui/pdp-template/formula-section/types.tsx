export type Theme = {
  darkColor: string;
  middleColor: string;
  lightColor: string;
};

export type DescriptionContent = {
  title: string;
  desc?: Array<string>;
  descHtml?: string;
  subDesc?: Array<string>;
};

export type Benefices = {
  title?: string;
  desc?: string;
  image?: string;
};
